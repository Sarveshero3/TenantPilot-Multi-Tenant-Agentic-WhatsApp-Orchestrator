# Handover — TenantPilot

> This file reflects CURRENT status only (overwrite each update, don't append).

## ⚠️ CRITICAL: Terminal Commands

**Antigravity IDE uses PowerShell by default. DO NOT run PowerShell commands.**
Prefix ALL terminal commands with `cmd /c` to force cmd.exe. Example:
```
cmd /c "git add . && git commit -m \"message\""
```
See `lessons.md` L-002 for details.

## Done

- Initialized project state files (`agent.md`, `lessons.md`, `handover.md`)
- Read assignment PDF in full
- Wrote Implementation Plan (see `agent.md`)

## In Progress

- Nothing currently in progress — awaiting user approval of the plan.

## Next Step (exact)

- Phase 1 (this phase) is **complete**: Implementation Plan is written in `agent.md`.
- **HANDOFF REQUIRED** — switch to Claude Sonnet 4.6 (Thinking) for Phase 2: DB schema + models (Task 1).

## Known Broken / Blocked

- No code written yet — nothing to break.
- Meta WhatsApp Business sandbox approval status unknown — will use mock mode until confirmed.
- Frontend deployment target is an **open decision** — do NOT pick a platform until asking the human.

## How to Run Locally Right Now

Nothing to run yet — only the implementation plan exists.

## WhatsApp Mode

**Mock mode** — no real Meta credentials wired in. The mock client will log exact Graph API payloads to console.

## Model/Editor That Did This Work

- **Claude Opus 4.6 (Thinking)** via Antigravity IDE
- Phase 1: Read PDF + write Implementation Plan
